<h3><%= @cocktail.name %></h3>
<ul>
  <% @cocktail.doses.each do |dose| %>
  <li>
    <%= dose.ingredient.name %>: <%= dose.description %> ml <%= link_to '(delete)', dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %>
  </li>
  <% end %>
</ul>

<%= render 'doses/new', cocktail: @cocktail, dose: @dose %>
